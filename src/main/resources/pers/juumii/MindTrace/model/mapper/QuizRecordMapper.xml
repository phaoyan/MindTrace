<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.juumii.MindTrace.model.mapper.QuizRecordMapper">

    <resultMap id="resultMap" type="QuizRecord">
        <id column="id" property="id"/>
        <result column="description" property="desc"/>
        <result column="completion" property="completion" />
        <result column="time" property="time" />
        <result column="task_id" property="taskId" />
    </resultMap>
    <insert id="insert">
        insert into quiz_record (id, description, completion, time, task_id)
        values (#{data.id}, #{data.desc}, #{data.completion}, #{data.time}, #{data.taskId})
    </insert>
    <update id="update">
        update quiz_record set
        description = #{data.desc},
        completion = #{data.completion},
        time = #{data.time},
        task_id = #{data.taskId}
        where id = #{data.id}
    </update>
    <delete id="deleteById">
        delete from quiz_record where id = #{id}
    </delete>
    <select id="queryByTaskId" resultMap="resultMap">
        select * from quiz_record where task_id = #{taskId}
    </select>
    <select id="queryAll" resultMap="resultMap">
        select * from quiz_record
    </select>
    <select id="queryById" resultType="pers.juumii.MindTrace.model.data.QuizRecord">
        select * from quiz_record where id = #{id}
    </select>
</mapper>