<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.juumii.MindTrace.model.mapper.LearningRecordMapper">

    <insert id="insert">
        insert into learning_record (id, description, completion, time, card_id)
        values (#{data.id}, #{data.description}, #{data.completion}, #{data.time}, #{data.cardId})
    </insert>
    <update id="update">
        update learning_record set
        description = #{data.description},
        completion = #{data.completion},
        card_id = #{data.cardId},
        time = #{data.time}
        where id = #{data.id}
    </update>
    <delete id="deleteById">
        delete from learning_record where id = #{id}
    </delete>
    <select id="queryByTaskId" resultType="learningRecord">
        select * from learning_record where card_id = #{cardId}
    </select>
    <select id="queryAll" resultType="learningRecord">
        select * from learning_record
    </select>
    <select id="queryById" resultType="learningRecord">
        select * from learning_record where id = #{id}
    </select>
</mapper>